/*
  this is the solution file for the needle in a haystack problem
  in leetcode using the KMP algorithm
*/

#include <stdio.h>
#include <string.h>

int KMP(char* needle, char* haystack);

int main(){

}

int KMP(char* needle, char* haystack) {
  if(needle == "") {
    return 0; //the first matching char will be the first one
  }

  //populate the LPS
  array
  int needle_length = strlen(needle);
  int lps[needle_length];

  for(int i = 0; i<needle_length; i++) {

  }

}
